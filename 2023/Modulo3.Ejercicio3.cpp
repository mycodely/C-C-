/* 
3. Leer 100 números enteros e imprimir la suma de ellos.
 */

#include <stdio.h>
int num, aux, aux2;

int main (){
    
num = 1;
aux = 0;

// Test with low number
    while(num<=4{
    printf("ingrese un nuevo entero\n");
    scanf("%d", &num);fflush(stdin);
    aux = num+ aux;
    num++;
    }
    printf("la suma se acumula: %d \n", aux);                                       ;
              
    getchar();
    return 0; 
    }
